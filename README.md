## How to Run
1. Ensure you have the required dependencies. Both Notebooks include a %pip install cell which will install the requirements.
2. Restart the Kernel
3. Execute the cells in Python 3.11.4, (I have worked in Deepnote and VSCode).
